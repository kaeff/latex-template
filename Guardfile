# A sample Guardfile
# More info at https://github.com/guard/guard#readme
guard 'process', :name => 'generate', :command => 'script/generate' do
  watch('Inhalt.md.txt')
  watch('Head.tex')
  watch('Literatur.bib')
  watch('template/Arbeit.tex')
end
